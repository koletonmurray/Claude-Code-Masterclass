rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TEST MODE: open access until 2026-06-06 — lock down before going to production
      allow read, write: if request.time < timestamp.date(2026, 6, 6);
    }
  }
}